# GEMINI.md

- Make minimal, targeted edits only; never rewrite large blocks
- Do not modify files outside the requested scope
- Do not change formatting or existing structure unless required
- Fix compilation errors before any functional change
- Preserve platform-specific behavior (Android/iOS/Desktop)
- Do not introduce breaking changes without clear reason
- Keep changes consistent with existing architecture and patterns
- Prefer explaining over guessing when unsure
- Ensure code compiles after each change
- Avoid unnecessary dependencies or refactors